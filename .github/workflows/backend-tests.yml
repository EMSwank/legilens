name: Backend Tests

on:
  pull_request:
    branches:
    - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install fastapi "sqlalchemy[asyncio]" asyncpg alembic pydantic pydantic-settings datasketch redis httpx apscheduler slowapi
        pip install pytest pytest-asyncio pytest-mock
    - name: Run tests
      run: |
        cd backend
        python -m pytest tests/ -x -q
